= simple_form_for [:admin, @your_support] do |f|
  = f.error_notification

  .form-inputs
    = f.input :text, input_html: { class: 'form-control full-width summernote' }
    %br

  .form-actions
    = f.button :submit, class: 'form-control full-width btn btn-default'
